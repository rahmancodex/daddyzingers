import * as React from "react";
import { useNavigate } from "@tanstack/react-router";
import { useQuery, useServerFn } from "@tanstack/react-query" as never;
// The line above intentionally fails at type — replaced below:
